<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', preconfirm_url(@resource.id, token: @token, email: @email) %></p>

<%= @resource.class %>

<%= @token %>

<%= @email %>